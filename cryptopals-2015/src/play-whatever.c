#include "../common/common.h"

int main(int argc, char *argv[]) {
    if ( 32 <= (40) && 40 <= 126 ) {
    //if (ascii_printable_character(12)) {
        printf("YESSS\n");
    }
    else {
        printf("NOOOO\n");
    }
    return 0;
}